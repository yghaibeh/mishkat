/**
 * القائمة البيضاء المغلقة للمسارات العامة المعلنة — CR-001 §ج (قب-١٣).
 *
 * أي إضافة إلى هذه القائمة **تغيير جوهري بـCR** — وبوابة G16 تفشل عند تجاوز السقف
 * المعتمد، فلا تنمو المسارات العامة صامتة.
 *
 * القيود الأربعة الملزمة على كل عضو:
 *  ١. عضويةٌ في هذه القائمة (تُراجع كوحدة).
 *  ٢. بوابة G16 تمنع النمو.
 *  ٣. **يكتب كياناً واحداً بحالة معلقة، ولا يقرأ بيانات شبكية إطلاقاً** — لا قوائم
 *     ولا تقارير ولا بحث (هذا ما يمنع تكرار أ-١: البحث المكشوف لمجهول في v1).
 *  ٤. ضوابط ق-٣٢ إلزامية (honeypot · سقف معدل · تحقق) + تدقيق كامل لكل طلب.
 */
export const PUBLIC_DECLARED_ROUTES = Object.freeze([
  "competition.publicEnroll",
  "registration.publicRequest",
])
