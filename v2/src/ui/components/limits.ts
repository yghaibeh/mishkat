/**
 * حدودُ العرض التخطيطية — SPEC_design_system §١-٦/ق-١١٦.
 *
 * ليست إعداداتٍ حيّة (قب-٦) ولا أرقامَ عمل: حدُّ الفصل المعلن في §١-١ هو «هل يغيّرها الأدمن
 * لتغيير **سلوك العمل**؟» — لا؛ فهي حدودُ تصييرٍ بصريّ. ومع ذلك تُجمَع هنا بمصدرٍ واحد
 * فلا تتناثر أرقاماً في المكوّنات (نظيرُ قاعدة الرمز على البنية لا على اللون).
 */

/** عتبةُ تحوّل الشجرة إلى تحميلٍ كسول (~٥٠٠ ورقة — ق-١١٦). */
export const TREE_LAZY_THRESHOLD = 500

/** أقصى حجمِ ملفٍّ مرفوعٍ من الواجهة؛ الخادمُ يحرسه أيضاً (المادة ٨/٤ — الواجهة لا تحمي). */
export const UPLOAD_MAX_BYTES = 5_000_000
