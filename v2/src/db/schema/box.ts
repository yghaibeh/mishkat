/**
 * جداولُ **الصندوق ومالية المسجد** (`features/box`) — السلسلةُ المالية (الهجرة `0013`).
 *
 * > **والثابتُ المفروضُ في المخطط**: **لا عمودَ مبلغٍ ولا عملةٍ ولا رصيد** (ق-٦٠) — المالُ
 * > كلُّه في دفتر النواة، وسجلُّ التسليم يحمل **معرّفَ القيد** ومنه يُقرأ كلُّ رقم.
 * > ويحرس ذلك `tests/migrations/box.test.ts` على المخطط المطبَّق لا على النيّة.
 *
 * > **ومفتاحا التوجيه صنفان لا صنفٌ واحد** (وصفة §٤-٠): القاموسُ **مرجعٌ شبكيٌّ** بالجذر `/`
 * > (النمط ب)، والتسليمُ **تشغيليٌّ بالوحدة** (النمط أ) وتوجيهُه **وحدةُ الوجهة** — وهي
 * > أعمقُ المسارين بحكم ق-٦١ (الوجهةُ محتواةٌ في المصدر احتواءً صارماً). ولذلك **مصنعان**
 * > في `boxRepository.ts`، فلا تفرض قراءةُ القاموس بالجذر حملَ تسليمات الشبكة كلِّها.
 *
 * > **والجدولان ملحقان فقط** (المادة ٧/٤): إيقافُ الفئة **بيانٌ** (`active = 0`) لا حذف
 * > (ق-٦٤)، وإقرارُ التسليم **تحديثٌ على الصفّ نفسِه** (ق-٦١) لا حذفٌ وإدراج.
 */

import { int, routing, text, TENANT_COLUMN, type TableSpec } from "./columns.js"

export const BOX_TABLES: readonly TableSpec[] = [
  {
    /**
     * **القاموسُ المغلق المركزيّ** (ق-٦٤) — بياناتٌ مرجعيةٌ بجذر الشبكة، تُدار ولا تُنشر
     * كوداً. **ملحقٌ فقط**: الفئةُ المُوقَفة تبقى صفّاً ليُفرَّق «أُوقفت» عن «لم تُسجَّل».
     */
    name: "box_categories",
    columns: [...routing(), text("id"), text("ar"), text("account_id"), int("active")],
    primaryKey: [TENANT_COLUMN, "id"],
    appendOnly: true,
    infrastructure: false,
  },
  {
    /**
     * **سجلُّ التسليم النازل** (ق-٦١) — بصمتان: مَن سلّم ومَن أقرّ. توجيهُه **وحدةُ الوجهة**
     * وهي `unit_path` نفسُه — **فلا عمودَ `to_unit_path`** (عمودان لقيمةٍ واحدةٍ مصدرا حقيقة).
     * **ملحقٌ فقط**: الإقرارُ تحديثٌ على الصفّ (`acknowledged_by`/`acknowledged_at`)، ونمذجتُه
     * حذفاً وإدراجاً كانت تُسقط «مَن سلّم» — وهو نصفُ ما وُجد السجلُّ له.
     */
    name: "box_handovers",
    columns: [
      ...routing(),
      text("id"),
      text("entry_id"),
      text("from_unit_path"),
      text("to_custodian_person_id"),
      text("handed_over_by"),
      int("at"),
      text("acknowledged_by", true),
      int("acknowledged_at", true),
    ],
    primaryKey: [TENANT_COLUMN, "id"],
    appendOnly: true,
    infrastructure: false,
  },
]
