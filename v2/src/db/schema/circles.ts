/**
 * جداولُ **نموذج الحلقات الموحّد** (`features/circles`) — الهجرة `0010` من موجة T31.
 *
 * > **والفخُّ المتوقَّع هنا كان «جدولاً لكل نوع» لا G23**: v1 كان يحمل `tahfeez_circles`
 * > و`halaqat` وسجلَّي طلابٍ يخيطهما جسر (ق-٨٨) — فأنتج ع-٢ («لماذا مكررة مرتين؟») وع-١٩
 * > وع-٢٩. **والعلاجُ في المخطط لا في الحارس**: جدولُ حلقةٍ **واحد** ونوعُه عمودٌ عليه،
 * > وجدولُ التحاقٍ **واحد** — فلا جسرَ يُبنى لأنّ لا انفصالَ يُخاط.
 *
 * ### مفاتيحُ التوجيه — لكلِّ جدولٍ سطرُه (ع-٥ · README الحسم ٢)
 *  · **الحلقةُ** كيانٌ ذو موطنٍ تنظيميّ ⟵ `unit_path` مسارُ وحدتها، **وهو مستقرٌّ بنيوياً**:
 *    مدخلُ التعديل ثلاثةُ حقولٍ ليس فيها وحدة، فلا مقبضَ ينقل حلقةً أصلاً (الوصفة فخّ ٤).
 *  · **الالتحاقُ** يرث موطنَ **حلقته** (نظيرُ حركةِ العُهد من أصلها) — يُشتقّ ولا يُخترع،
 *    ولا يُوجَّه إلى الجذر صامتاً.
 *  · **الوحداتُ** إسقاطٌ لاشتقاق النطاق (نظيرُ `ledger_units`) ⟵ `unit_path` مسارُها نفسُه.
 *  · **كتالوجُ الأنواع** بياناتٌ مرجعيةٌ **نطاقُها الشبكةُ كلُّها** (قب-٢٢/ق-٨٩) ⟵
 *    `unit_path = '/'` **صادقٌ لا حشو**: معجمٌ واحدٌ للشبكة لا يخصّ شظيةَ وحدة.
 *
 * ### أيُّها ملحقٌ فقط (`appendOnly`) — سؤالٌ لكلِّ جدولٍ لا حكمٌ للوحدة (README §٤-٣)
 *  · **الحلقة**: بياناتُ عملٍ حسّاسة — الأرشفةُ **وسمٌ لا محو** (المادة ٧/٤).  ⟵ **ملحقٌ فقط**.
 *  · **الالتحاق**: سجلُّ عضويةٍ لا يُمحى (ق-٣١)، والخروجُ **وسمٌ على الصفّ نفسِه**
 *    (`stampLeft`) لا حذفٌ وإدراج.  ⟵ **ملحقٌ فقط**.
 *  · **الوحداتُ والكتالوج**: نسخةُ قراءةٍ وبياناتٌ مرجعية يجوز أن يزول صفُّها (نظيرُ
 *    `ledger_units`/`library_categories`).  ⟵ **ليست ملحقاً**.
 */

import { int, routing, text, TENANT_COLUMN, type TableSpec } from "./columns.js"

export const CIRCLES_TABLES: readonly TableSpec[] = [
  // ── T31: نموذجُ الحلقات الموحّد (الهجرة `0010`) ────────────────────────────
  {
    /** إسقاطُ الوحدة لاشتقاق النطاق — نسخةُ قراءةٍ لا مصدرُ حقيقة (نظيرُ `ledger_units`). */
    name: "circles_units",
    columns: [...routing(), text("id")],
    primaryKey: [TENANT_COLUMN, "id"],
    appendOnly: false,
    infrastructure: false,
  },
  {
    /**
     * **نوعُ الحلقة** (ب-٢٨/قب-٢٢) — بياناتٌ مرجعيةٌ نطاقُها الشبكةُ كلُّها، تُضاف صفّاً فتعمل.
     * **وثلاثةُ أعمدةٍ لا رابعَ لها**: لا `active` ولا `enabled` — بابُ المنع الثاني الذي
     * أنتج ع-٨ **غيرُ موجودٍ ليُنسى إغلاقُه** (قب-٤٠: يُحرَس بغياب الحقل لا بالانضباط).
     */
    name: "circles_types",
    columns: [...routing(), text("id"), text("ar")],
    primaryKey: [TENANT_COLUMN, "id"],
    appendOnly: false,
    infrastructure: false,
  },
  {
    /**
     * **الحلقة** (IA ك-١) — كيانٌ واحدٌ ونوعُه **عمودٌ** لا جدولٌ ثانٍ (ب-٢٨).
     * **ملحقٌ فقط**: الأرشفةُ وسمٌ (`archived_at`) لا محو، وموطنُها `unit_path` **لا يتحرّك**
     * (لا مقبضَ نقلٍ في مدخل التعديل) ⟵ صفوفُ التحاقها الملحقةُ ترث مساراً مستقرّاً.
     * و`teacher_person_id` **إسنادٌ حقلٌ عليها** لا صفُّ ربطٍ ثانٍ — فإسنادان لشخصٍ واحدٍ
     * لا يُنتجان صفّين (ع-٢٩)، والقدرةُ الشخصيةُ تُفرَض في المحرّك لا هنا (قب-٣٨).
     */
    name: "circles_circles",
    columns: [
      ...routing(),
      text("id"),
      text("type_id"),
      text("name_ar"),
      int("capacity"),
      text("teacher_person_id", true),
      int("archived_at", true),
      int("created_at"),
    ],
    primaryKey: [TENANT_COLUMN, "id"],
    appendOnly: true,
    infrastructure: false,
  },
  {
    /**
     * **التسجيل/الطالب** (IA ك-٢ · ق-٣١) — **سجلٌّ واحدٌ** ابنُ الحلقة باسمٍ حرٍّ بلا هوية:
     * ليس فيه `person_id`، فليس كلُّ مذكورٍ مستخدماً والحسابُ يُنشأ في `org` عند الحاجة.
     * **ملحقٌ فقط**: الخروجُ وسمٌ (`left_at`) **يُحدَّث على الصفّ نفسِه** لا حذفٌ وإدراج.
     * ونطاقُه **يرث موطنَ حلقته** فلا يُخترع (circlesRepository).
     */
    name: "circles_enrollments",
    columns: [
      ...routing(),
      text("id"),
      text("circle_id"),
      text("name_ar"),
      int("joined_at"),
      int("left_at", true),
    ],
    primaryKey: [TENANT_COLUMN, "id"],
    appendOnly: true,
    infrastructure: false,
  },
]
