/**
 * الواجهاتُ المستعارة — **اعتمادٌ بعقدٍ لا باستيراد وحدةٍ أخرى** (المادة ٣/٢، عقدُ الوحدة §٩).
 *
 * مصفوفةُ المتابعة تحتاج جواباً لا تملكه هذه الوحدة: **«مَن في هذا النطاق؟»** — وهو سؤالُ
 * **إسنادٍ** موطنُه وحدةُ التنظيم (IA ك-١٣). فيُعلَن هنا واجهةً تُحقن في سياق الطلب، ولا
 * تستورد الوحدةُ مجلدَ وحدةٍ أخرى (قب-٣١: كلٌّ في مجلده).
 *
 * **والواجهةُ تُعطي وقائعَ لا أحكاماً**: تُعيد أسماءَ أشخاصٍ في نطاق، **والحكمُ** بمن تبلغه
 * المادةُ يُحسب في `reach.ts` وحده — فتبقى القاعدةُ في موضعٍ واحدٍ لا يقرّرها كلُّ رافدٍ
 * باجتهاده (وهو عينُ مرض v1: المنطقُ مبعثر).
 */

export type LibraryPorts = {
  /** مَن له تكليفٌ فعّالٌ داخل هذا المسار — **مساراتٌ لا أدوار** (G6). */
  readonly peopleIn: (unitPath: string) => readonly string[]
}
