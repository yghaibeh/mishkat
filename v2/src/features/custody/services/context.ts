/**
 * سياقُ خدمات العُهد — **يُحقن ولا يُستورد** (`SPEC_settings` §١-٨، TESTING_POLICY §٥).
 *
 * ثلاثةُ مدخلاتٍ لا رابعَ لها: **الساعةُ** من الطلب (فلا `Date.now()` في خدمة)،
 * و**الفاعلُ** من الجلسة (فلا «من نفّذ» من مدخل العميل — ق-٧٨)، و**سؤالُ بلوغ النطاق**
 * للمستلِم (ق-٨١) جوابُه من مسار التكليف لا من اسم دور (G6).
 *
 * **ولا مُحلِّلَ إعداداتٍ هنا**: هذه الوحدة **بلا رقمٍ تشغيليٍّ واحد** — لا سقفَ ولا عتبةَ
 * ولا مدّة — فلا بندَ لها في سجل الإعدادات ولا حاجةَ لحقنه (قب-٦/G14).
 */

import type { ScopeReach } from "./directory.js"

export type CustodyContext = {
  readonly now: Date
  readonly actorPersonId: string
  readonly reaches: ScopeReach
}
