إثباتُ حرّاس T22 بالكسر — ٩ ثوابتٍ كُسرت بيدي، وسُجِّل ما سقط
(المنهج: قب-١٥ «البوابةُ تُثبَت بالفشل لا تُدّعى» · وقب-٤٠ «دعوى بنيوية تُقاس بالمحتوى»)
═════════════════════════════════════════════════════════════════════════════

✗ ١) الموطنُ يعود مزدوجاً: أُعيد «lesson» إلى canonicalHome في شاشة الدروس
     ⇒ G20 حمراءُ بنصّ CR-016 حرفياً:
       «lesson» موطنُه /mosque/circles/log و/mosque/circles/lessons
     (وسقط معها T22/٣ — فالحارسان يقولان الشيءَ نفسَه من موضعين)

✗ ٢) حارسُ الشكل يُنزع من الكاتب الواحد (identify: شرطُ المطابقة مُزال)
     ⇒ سقط T22/٤: «شكلُ التحفيظ على حلقةٍ نوعُها ذو منهاج ⇒ SESSION_SHAPE_MISMATCH»
     — أي أنّ «الحقولُ تتبع النوع» **يُحرَس فعلاً** لا يُوصَف.

✗ ٣) الشكلُ يُصلَّب في الكود بدل صفوف البيانات (شرطٌ على معرّف النوع بدل سؤال الكتالوج)
     ⇒ سقط T22/٤: «منهاجٌ يُضاف صفّاً ⇒ الشكلُ ينقلب فوراً بلا سطر كود»
     — وهو حارسُ ع-٨ نفسُه في طبقة السجل.

✗ ٤) ملفُّ وصلٍ **ثانٍ** إلى موطن الكيان (__violation__.ts يستورد DaySession)
     ⇒ سقط T22/١: expected [ …(2) ] to deeply equal [ 'education/services/dayLogPort.ts' ]

✗ ٥) ملفُّ وصلٍ **بصفر** استيراد (عُلِّقت استيراداتُ موطن الكيان كلُّها)
     ⇒ سقط T22/١: expected [] to deeply equal [ 'education/services/dayLogPort.ts' ]
     — **الحارسُ يفشل عند اثنين وعند صفرٍ معاً**: صفرٌ معناه أنّ الكيانَ استُنسخ لا وُصل.

✗ ٦) جسرُ مزامنةٍ يُبعث بين الوحدتين (syncDaySessions)
     ⇒ سقط T22/٢: «لا مقبضَ جسرٍ ولا مزامنةٍ ولا توأمةٍ ولا مرآةٍ في الوحدتين معاً»
     — ق-٨٨ متقاعدٌ لا منقول، ولو في طبقةٍ أدنى.

✗ ٧) عدّادٌ مخزَّنٌ يُزرع في كيان الجلسة (presentCount في types.ts)
     ⇒ سقط T22/٥: «عدّادٌ مخزَّن: circleLog/types.ts:93 — readonly presentCount: number»

✗ ٨أ) مسارٌ ثانٍ يُفوّض كتابةَ اليوم (ملفٌّ آخر ينادي days.record)
     ⇒ سقط حارسُ وحدة التعليم: expect(delegators).toEqual(["services/lessons.ts"])

✗ ٨ب) مستودعُ جلساتٍ ثانٍ في وحدة التعليم (ملفٌّ ينادي upsertSession)
     ⇒ سقط T22/١ وحارسُ الوحدة معاً: الكاتبُ واحدٌ **في موطنه** لا واحدٌ في كل وحدة.

⚠ مسبارٌ أخطأ ثم صُحِّح (يُسجَّل لأنه درس):
   أولُ مسبارٍ للبند ٨ زرع سطراً لا يكتب شيئاً (`void store.curricula.length`) فمرّ الحارس —
   **والخطأُ في المسبار لا في الحارس**: لا مخالفةَ في السطر أصلاً. أُعيدت الصياغةُ بمخالفةٍ
   حقيقية (كاتبٌ ثانٍ ومفوِّضٌ ثانٍ) فسقط الحارسان. (درسُ قب-٤٠ على نفسي: حين يمرّ الحارسُ
   اسأل أولاً «أمسباري خاطئ؟».)

وأُعيدت الشجرةُ إلى حالها بعد كل كسر — و٢١ بوابة خضراء بعده (GATES-GREEN-T22.txt).
