# الكسراتُ الموجَّهة (قب-٤٧) — **على الطقم كلِّه** لا على مجلد الوحدة

### الثابت: **الذرّية** — الدفعةُ ترتدّ عند الفشل (`ROLLBACK`)
المكسور: `src/db/sql/sqliteDriver.ts`
الساقط: 1 اختباراً · منها **1** يحمل اسمَ الثابت
الأسماءُ الحاملةُ لاسم الثابت (أوّلُ ثلاثة):
  - الذرّية: فشلُ عبارةٍ في الدفعة لا يترك أثراً جزئياً من سابقاتها
G10: ✓ G10 — الهجرات ومفتاح التوجيه (1 هجرة مُختبَرة · 13 جدولَ بياناتٍ كلُّها تحمل مفتاح التوجيه)

### الثابت: **الذرّية** — الفارقُ يُحسب من الحالة النهائية (فيرتدّ مع الذاكرة)
المكسور: `src/db/unitOfWork.ts`
الساقط: 1 اختباراً · منها **1** يحمل اسمَ الثابت
الأسماءُ الحاملةُ لاسم الثابت (أوّلُ ثلاثة):
  - الذرّية: محوُ صفٍّ من جدولٍ ملحقٍ فقط يُرمى ولا يُترجم إلى `DELETE` (المادة ٧/٤)
G10: ✓ G10 — الهجرات ومفتاح التوجيه (1 هجرة مُختبَرة · 13 جدولَ بياناتٍ كلُّها تحمل مفتاح التوجيه)

### الثابت: **عزلُ الشبكة** — المستودعُ مقسَّمٌ بالشبكة في كل قراءة
المكسور: `src/db/unitOfWork.ts`
الساقط: 4 اختباراً · منها **4** يحمل اسمَ الثابت
الأسماءُ الحاملةُ لاسم الثابت (أوّلُ ثلاثة):
  - عزلُ الشبكة: صفٌّ من شبكةٍ أخرى **لا يُقرأ** ولو تطابق المسارُ حرفياً
  - عزلُ الشبكة: كتابةُ شبكةٍ **لا تمسّ** صفَّ الأخرى ولو حمل المعرّفَ نفسَه
  - عزلُ الشبكة: رصيدُ الشبكة يُحسب من أسطرها وحدها
G10: ✓ G10 — الهجرات ومفتاح التوجيه (1 هجرة مُختبَرة · 13 جدولَ بياناتٍ كلُّها تحمل مفتاح التوجيه)

### الثابت: **مفتاحُ التوجيه** — فهرسُ (شبكة، مسار) على كل جدول
المكسور: `src/db/migrations/0001_org_ledger_pilot.sql`
الساقط: 1 اختباراً · منها **1** يحمل اسمَ الثابت
الأسماءُ الحاملةُ لاسم الثابت (أوّلُ ثلاثة):
  - مفتاحُ التوجيه: كلُّ جدولٍ مفهرسٌ على (شبكة، مسار) — فاستعلامُ «ما تحت العقدة» لا يمسح
G10: • جدولُ بياناتٍ بلا فهرسِ توجيه: journal_lines ينقصه فهرسٌ على (tenant_id, unit_path)

### الثابت: **مفتاحُ التوجيه** — عمودُ `unit_path` على كل جدولِ بيانات
المكسور: `src/db/migrations/0001_org_ledger_pilot.sql`
الساقط: 64 اختباراً · منها **6** يحمل اسمَ الثابت
الأسماءُ الحاملةُ لاسم الثابت (أوّلُ ثلاثة):
  - مفتاحُ التوجيه: لا صفَّ بيانات شبكةٍ بلا مسارٍ بعد النقل
  - مفتاحُ التوجيه: كلُّ جدولٍ يحمل بيانات شبكةٍ يحمل `tenant_id` و`unit_path`
  - مفتاحُ التوجيه: كلُّ جدولٍ مفهرسٌ على (شبكة، مسار) — فاستعلامُ «ما تحت العقدة» لا يمسح
G10: • جدولُ بياناتٍ بلا مفتاح توجيه: journal_lines ينقصه unit_path (ع-٥)
