# التبعيات والبناء
node_modules/
dist/
.output/
.nitro/
.vite/

# أسرار محلية وحالة wrangler — لا تُرفع أبدًا
.dev.vars
.wrangler-state/
.wrangler/

# سجلّات ومتنوّعات
*.log
.DS_Store

# توكن نشر Cloudflare (لا يُرفع)
.cloudflare.env

# ملفات العميل الكبيرة (APK) — تبقى محليًا خارج git
from_client/

# نسخ احتياطية محلية للإنتاج — لا تُرفع
backups/prod_backup*.sql

# نواتج بناء v2 (تغطية/أدلة مؤقتة) — لا تدخل التاريخ
v2/coverage/

# رابطُ node_modules التُزم خطأً في فرع T19 (رابطٌ يشير إلى نفسه يكسر أيَّ نسخة جديدة)
v2/node_modules
