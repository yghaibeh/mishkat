# سجل جاهزية المنصّة — Gap Register

> أداة إدارة حية على نمط *Production Readiness Review* (Google) و*Well-Architected* (AWS).
> كل بند: **الحالة** (✅ مغطّى · 🟡 جزئي · ⛔ ثغرة) · أين · متى يُعالَج.
> لا يُطلق أول إنتاج فعليّ (المرحلة ٥) وفي القائمة بندٌ ⛔ لم يُقرَّر مصيره.
> يُراجَع في كل انتقال مرحلة. مالكه: مدير البرنامج.

## العمود ١ — الأمن (Security)
| البند | الحالة | أين / الخطة |
|---|---|---|
| التخويل (Authorization) | ✅ | `SPEC_authorization` + محرك can() + المصفوفة + G5/G6/G7 |
| المصادقة ودورة الجلسة | 🟡 | مبادئ في ق-٢٢…ق-٢٦؛ **تُفصَّل في مواصفة org/accounts (T4A)** |
| إدارة الأسرار | ✅ | ENGINEERING §٨ + G11 |
| **نمذجة التهديد (Threat Model STRIDE)** | ⛔ | **ثغرة** — تُكتب مواصفة تهديد قبل المرحلة ٥ |
| **تصنيف البيانات وحماية بيانات القُصَّر** | ⛔ | **ثغرة عالية الأثر** — النظام يحمل بيانات طلاب قُصَّر + روابط أولياء (ق-٩٣). يلزم سياسة صريحة |
| تحديد المعدل ومنع الإساءة | 🟡 | ق-٣٢ + المسار العام (قب-١٣)؛ يُعمَّم في مواصفة الأمن |
| مراجعة أمنية دورية / اختبار اختراق | ⛔ | **ثغرة** — تُجدوَل قبل الإنتاج |
| سجل التدقيق (كتابة) | ✅ | ENGINEERING §٤/٨؛ (القراءة/العمر في ADR-001 §٧) |

## العمود ٢ — الموثوقية (Reliability)
| البند | الحالة | أين / الخطة |
|---|---|---|
| النسخ والاسترجاع (Backup/RTO) | 🟡 | ADR-001 §٧-٢ (مُسمّى، يحتاج تفصيلاً تنفيذياً) |
| **استجابة الحوادث + تشريح بلا لوم (Postmortem)** | ⛔ | **يبقى ثغرة** — مستويات الإنذار ومتلقّوها وزنادُ التشريح (٤٨ ساعة، نفس مهلة «طارئ») في `SPEC_observability` §٣-٢/§٥-٤؛ **والعمليةُ الكاملة** (تصعيد، مناوبة، تواصل مستخدمين) لم تُكتب ⟵ مهمة ط-١٠ |
| **كتيّبات التشغيل (Runbooks)** | 🟡 | الثلاثة الأولى **هيكلاً** (سقوط القاعدة · امتلاء سقف G23 · فشل هجرة) في `SPEC_observability` §٥؛ النصُّ الكامل ⟵ ط-٤، والتمرين عليها ⟵ ط-٥ (الحارسان م-١٢/م-١٣) |
| التعافي من الكوارث (DR) | 🟡 | ADR-001 (عتبات الهجرة)؛ يحتاج خطة DR كاملة |
| تدرّج الطرح والتراجع | 🟡 | مفاتيح التفعيل (قب-٧) أساسٌ ممتاز؛ يلزم استراتيجية بيئات |

## العمود ٣ — التشغيل والمراقبة (Operations)
| البند | الحالة | أين / الخطة |
|---|---|---|
| **المراقبة والقياسات (Observability/SLO/SLI)** | 🟡 | **`SPEC_observability.md` مكتوبة (T29، مسودة للاعتماد)**: ٧ مؤشرات بأهدافها ومصادرها ومتلقّيها · حدُّ خطأ شهريّ ٢١٦ دقيقة · قائمةُ يوم النشر التجريبيّ · ١٣ حارساً مقترحاً. **العقد قائم والتنفيذ مهامٌّ لاحقة** (§٧) |
| تتبّع الأخطاء (Error tracking) | 🟡 | `SPEC_observability` §٣-٣/٣-٤: **بديلٌ مجانيٌّ أولاً** (Workers Logs + Analytics Engine)، والأداةُ الخارجية **بندٌ مؤجَّلٌ بثمنه** — والثمنُ ﴿غيرُ متحقَّق﴾ يُسعَّر بمهمة ط-١١ |
| استراتيجية البيئات (dev/staging/prod) | 🟡 | نشر من CI (ENGINEERING §٩)؛ تلزم بيئة staging معلنة |
| أدوات الدعم (انتحال قرائي، تصحيح محكوم) | ✅ | ب-٤٠أ (قب-٩) + records.correct |
| تأهيل المطوّرين الجُدد + ملكية الكود (CODEOWNERS) | 🟡 | البروتوكول يحكم التغيير؛ **يلزم دليل مساهمة** (المالك: «أكثر من شخص سيعمل») |

## العمود ٤ — الأداء والتوسّع (Performance & Scale)
| البند | الحالة | أين / الخطة |
|---|---|---|
| توسّع طبقة البيانات | ✅ | ADR-001 (عتبات ١٠٠×) |
| ميزانية أداء المحرك | ✅ | SPEC_authorization §٤ (صفر استعلام) |
| **ميزانية أداء الواجهة (LCP/INP/حجم الحزمة)** | ⛔ | **ثغرة** — تُضاف لمواصفة نظام التصميم (T4B) |
| اختبار الحِمل (Load testing) | ⛔ | **ثغرة** — سيناريو ذروة الخميس (ADR §١-٥) يُختبر فعلاً قبل الإنتاج |
| **تعدّد الشبكات (Multi-tenancy)** | 🟡 | **قب-١٨**: مهيّأ للتعدد (جذر شبكة + tenant_id)، شبكة واحدة تشغيلاً — يُراجَع تسليم T4A |

## العمود ٥ — الخصوصية والامتثال (Privacy & Compliance)
| البند | الحالة | أين / الخطة |
|---|---|---|
| الاحتفاظ بالبيانات | 🟡 | قب-٦ (إعداد حي) + ب-٣٧أ (سنتان)؛ يحتاج سياسة كاملة |
| **موافقة/بيانات القُصَّر والأولياء** | ⛔ | **قب-١٩: تأجيل واعٍ** — يبقى ⛔ ويُعاد فرضه على المالك قبل المرحلة ٥ (لا يُطلق إنتاج وهو غير محسوم) |
| إقامة/سيادة البيانات | 🟡 | ADR-001 (استثناء يعيد فتح التقسيم)؛ مرتبط بتعدّد الشبكات |
| حقوق صاحب البيانات (حذف/تصدير) | ⛔ | **ثغرة** — أُجّل في ب-٣٧؛ يُقرَّر قبل الإنتاج |

## العمود ٦ — العميل والمنتج (Client & Product)
| البند | الحالة | أين / الخطة |
|---|---|---|
| **إصدار عقد الـAPI وتوافق العملاء القدامى** | ⛔ | **ثغرة — مهمة للجوال**: مستخدمو APK لن يحدّثوا معاً؛ الخادم يجب أن يخدم عملاء متأخّرين |
| مزامنة الأوفلاين وحلّ التعارض | 🟡 | ق ت-٨ + ب-٣٩هـ (زمن الخادم)؛ يحتاج دلالة تعارض صريحة (اتصالات الميدان) |
| **اختبار الانحدار البصري (Visual regression)** | ⛔ | **ثغرة تخصّ شكواك من الواجهة** — تُضاف لسياج الواجهة (G20 المحيطة) |
| تحليلات الاستخدام (Product telemetry) | ⛔ | ثغرة — واعية بالخصوصية؛ كيف نعرف ما يفعله المستخدمون فعلاً |
| تأهيل المستخدم النهائي والمساعدة | 🟡 | حالات الفراغ (ق-١١٢) + مكتبة تدريبية (ميزة)؛ يحتاج تدفّق أول تشغيل |
| الإشراف على المحتوى (Moderation) | 🟡 | نسبة الصورة (ق-١٠٣)؛ رفع مستخدمين يحتاج ضبطاً |

## العمود ٧ — الكلفة (Cost / FinOps)
| البند | الحالة | أين / الخطة |
|---|---|---|
| مراقبة الكلفة عند ٢٠× | 🟡 | ADR-001 قدّر كلفة Postgres؛ يحتاج ميزانية ورصداً مستمراً. **وكلفةُ المراقبة نفسِها** مؤشّرٌ معلَنٌ في `SPEC_observability` §٣-٥ (ينذر عند ٨٠٪ من المشمول — نفس مسطرة ع-٤)، **ومعدّلُ الطلبات الشهريّ غيرُ مقيس** فلا تُوقَّع كلفةٌ قبل ن+٣٠ |

## البنود العاجلة (Tier 1) — حالة القرار
1. **تعدّد الشبكات** — ✅ محسوم قب-١٨ (مهيّأ للتعدد، واحدة تشغيلاً؛ يُراجَع في T4A).
2. **حماية بيانات القُصَّر** — 🟡 قب-١٩ (تأجيل واعٍ؛ يُعاد فرضه قبل المرحلة ٥).
3. **المراقبة/SLO** — 🟡 **المواصفة سُلِّمت (T29، ٢٠٢٦-٠٧-٢٤)**: `specs/SPEC_observability.md` — مسودةٌ بانتظار اعتماد المالك/المدير، ومعها ١١ مهمّةً مقترحةً (§٧) و١٣ حارساً بترقيمٍ مؤقّت **يُحوَّل إلى سلسلة G عند الاعتماد** (لا يُدرَج صامتاً).
4. **إصدار عقد الـAPI** — 🗓️ مهمة المدير: يدخل عقد أول دالة خادم في T4A (مبدأ الإصدار من اليوم).
5. **اختبار الانحدار البصري** — 🗓️ مهمة المدير: يُبنى ضمن كود سياج الواجهة (محيط G20).

- **نشرُ المنهاج للعامة (CR-022-أ)**: تصديرٌ منشور باتجاهٍ واحد إلى الموقع العام — بعد استقرار
  الإطار (قب-٢٦)؛ **لا يفتح منفذ قراءةٍ عامّاً** بحكم مبدأ «يُنشَر ولا يُكشَف» (قب-٤٥).

### الديونُ المُصرَّحةُ التي تنتظر أول نشرٍ تجريبيّ — **مجموعةً في مكانٍ واحد**
> **جُمعت وفُصِّلت في `specs/SPEC_observability.md` §٢** (ما يُقاس · كيف · ماذا يترتّب عند المخالفة).
> و**ن = يومُ أول نشرٍ تجريبيّ**؛ ولا يُقاس شيءٌ في الإنتاج إلا **بإذنٍ صريح** (§٢-٣).

- **د-١ · مِقبضُ D1 الحقيقيّ غيرُ مُشغَّل (T25)**: المُبرهَن لهجةُ SQL والذرّيةُ والعزل على
  `node:sqlite`؛ وغيرُ المُبرهَن سلوكُ D1 الشبكيّ (الزمن، حدُّ الدفعة، أخطاء الشبكة).
  ⟵ يُقاس في أول نشرٍ تجريبيّ (قب-٤٨). **وعليه يُسنَد SLI-٤ (p95 الكتابة) لأول مرة.**

- **د-٢ · سقوفُ G23 الإحدى عشرة** — **وتصحيحٌ لصياغةٍ سابقةٍ هنا**: قب-٥٠/٣ وصف سقفَ العُهد
  (٢٠٬٠٠٠) بأنه «تقديرٌ لا قياس»، **ولا يصحّ تعميمُ الوصف على الأحد عشر**. قراءةُ المستودعات
  في T29 وجدت **كلَّ سقفٍ مُسنَداً إلى مرساةٍ مقيسةٍ من ADR-001 ومُقدَّراً في ضلعٍ صرّح به
  كاتبُه نصّاً** — فـ`org` و`ledger` أقواها إسناداً، و`media` و`library` أضعفُها ضلعاً.
  ⟵ **يُقاس الضلعُ الضعيفُ لكلٍّ باسمه** (`SPEC_observability` §٢-٢/د-٢)، **ويُقاس التوزيعُ لا
  المتوسّط** — المتوسّطُ يُخفي الذيلَ، والذيلُ هو ما يُفشل البناء.
  > *وهذا نفسُه درسُ قب-٥٠: الوصفُ المنقولُ ادّعاءٌ كالرقم المنقول — يُقرأ في مصدره أو يُطرح.*

- **د-٣ · حمولةُ صفّ `audit_log` (٣٠٠ بايت) ونصيبُه من النمو (٣٦٪)** — «أضعفُ رقمٍ في ADR-001»
  وأكبرُ مستهلكٍ للسعة (§١-٣، §٧ «أولُ ما يُقاس في الإنتاج»). **انحرافٌ ≥٢× ⟵ ADR-001 يُعاد
  فتحه بـCR** — شرطٌ مفعَّلٌ نصّاً بقب-١٤، لا يُعدَّل صامتاً.

- **د-٤ · معاملُ الانبثاق في `notifications`** (~٣ إشعاراتٍ لكل تقديم × ~١٫٥ سطرِ تسليم) —
  **دَينٌ صرّح به كاتبُه في تعليق `notificationsRepository.ts` ولم يصل هذا السجلَّ حتى T29**؛
  عليه يقوم سقفُ ٥٠٠٬٠٠٠ صفّاً. **ونطاقُه `/` لا يُضيَّق** ⟵ أولُ تجاوزٍ له هو **زنادُ CR-026**
  بعينه (قب-٤٨): يُعاد فتحه ويُنفَّذ التحويلُ إلى عقودٍ غيرِ متزامنة، **ولا يُرفع السقفُ صامتاً**.
  ⟵ يُقاس ن+٣٠ (معدّلٌ لا لقطة).
  > *ودَينٌ يعرفه ملفُّ كودٍ وحده ليس دَيناً مُصرَّحاً بل تعليقاً ينتظر قارئاً.*

- **تثبيتُ بيئة بناء الواجهة (قب-٥٣/٣)**: قياسُ (أ) تحرّك +٩٫٤٪ بترقية مصغِّر — أدواتُ
  الواجهة تُقفل و CI يبني بـ`npm ci`؛ يُنفَّذ مع أول مهمة واجهة.

## بنود المدير المجدولة (لا تحتاج قرار المالك — للعلم والتتبّع)
- تهديد STRIDE + تصنيف البيانات: مواصفة أمن قبل المرحلة ٥.
- استجابة الحوادث + Postmortem + Runbooks: تُضاف لـ CHANGE_PROTOCOL/تشغيل قبل الإنتاج —
  **وهياكلُ Runbooks الثلاثة ومستوياتُ الإنذار صارت في `SPEC_observability` §٣/§٥؛ الباقي ط-١٠**.
- **١١ مهمّةً مقترحةً من `SPEC_observability` §٧** (ط-١…ط-١١) — للمدير أن يُرتّبها عند الاعتماد.
  **وط-١ (إدراج مجال `observability` في `SPEC_settings` بـCR) أولاً إلزاماً**: بلا مفاتيحَ في
  السجلّ تُفشل G14 أولَ كودِ مراقبةٍ يُكتب.
- استراتيجية البيئات (staging) + تدرّج الطرح عبر مفاتيح التفعيل القائمة: مع أول نشر.
- دليل مساهمة المطوّرين + CODEOWNERS: قبل انضمام مطوّر ثانٍ.
- دلالة تعارض المزامنة، تحليلات واعية بالخصوصية، إشراف المحتوى، ميزانية أداء الواجهة: مجدولة بمراحلها.
