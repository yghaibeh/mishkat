شريط بحث حبّي مع صف فلاتر قابلة للتفعيل (تنزلق أفقياً على الجوال).

```jsx
<SearchBar value={q} onChange={setQ} filters={['الكل','متأخر','معلّق']} activeFilters={['الكل']} onToggleFilter={toggle} />
```
