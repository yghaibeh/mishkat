عنصر قائمة بصورة رمزية واسم وحالة؛ الذيل حرّ (Chip أو AttendanceSwitch أو سهم).

```jsx
<ListItem avatarName="أحمد الصفدي" title="أحمد الصفدي" subtitle="جزء عمّ" status={<Chip status="approved" />} chevron onClick={open} />
```
